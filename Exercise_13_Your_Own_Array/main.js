"use strict";
let transportation = ['car', 'motorcycle', 'train', 'cycle'];
let message = 'I would like to own a ';
for (let i = 0; i < transportation.length; i++) {
    console.log(message + transportation[i]);
}
